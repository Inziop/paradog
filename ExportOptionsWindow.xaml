<Window x:Class="ParadoxTranslator.ExportOptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Export Options" Height="220" Width="380"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#1E1E1E" Foreground="White">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Text="Choose export format:" FontWeight="Bold"/>
    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8,0,12">
      <RadioButton x:Name="CsvRadio" Content="CSV" IsChecked="True" Margin="0,0,16,0"/>
      <RadioButton x:Name="JsonRadio" Content="JSON" Margin="0,0,16,0"/>
      <RadioButton x:Name="YamlRadio" Content="YAML (Paradox)"/>
    </StackPanel>

    <StackPanel Grid.Row="2">
      <TextBlock Text="Scope:" FontWeight="Bold"/>
      <StackPanel Orientation="Vertical" Margin="0,8,0,0">
        <RadioButton x:Name="AllFilesRadio" Content="Export all open files" IsChecked="True"/>
        <RadioButton x:Name="SelectedFileRadio" Content="Only export selected file"/>
      </StackPanel>
    </StackPanel>

    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
      <Button Content="Cancel" Width="80" Margin="0,0,8,0" Click="OnCancel"/>
      <Button Content="Continue" Width="90" Background="#007ACC" Click="OnContinue"/>
    </StackPanel>
  </Grid>
</Window>
